START:
INP      ; Input a value and store it in the accumulator
CMA      ;Complements the accumulator value
OUT      ; Output the result
HLT      ; Halt execution
NUM: .data 1 0  ; Memory location labeled "NUM", initialized with 0
